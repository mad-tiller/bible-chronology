const BASE_URL = 'https://dev-api.tntbtbf.com';

export default {
  BASE_URL,
  // You can add more endpoints or config here if needed
  ENDPOINTS: {
    AUTHENTICATE: `${BASE_URL}/api/v1/user/authenticate`,
  },
};